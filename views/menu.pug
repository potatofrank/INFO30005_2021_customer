
doctype html
html
  head
    link(rel='stylesheet', href='/stylesheets/menu.css')
    block head
body
    
    a(href='/customer/menu/cart') Cart
    h1 COFFEE
    if(menu)
        each product,id in menu
            if(product.type==="COFFEE")
                p #{product.snack_name}
                p #{product.price}
                //p #{product.type}
                //p #{product.add_on}
                //p #{product.size}
                p #{product.mass}
                p #{product.calories}
                button.btn(type="plus", onclick='myFunction(\'' + id + '\')') +
                .product_detail
                    p #{product.snack_name}
                    p #{product.price}
                    p #{product.type}
                    p #{product.add_on}
                    p #{product.size}
                    span total price = 
                    span
                        script.
                            document.write(#{product.price});
                    form(action='/customer/menu', method='POST')
                        
                        button.btn(type="add") Add to cart
                br
    h1 BISCUIT
    if(menu)
        each product,id in menu
            if(product.type==="BISCUIT")
                p #{product.snack_name}
                p #{product.price}
                //p #{product.type}
                //p #{product.add_on}
                //p #{product.size}
                p #{product.mass}
                p #{product.calories}
                button.btn(type="plus", onclick='myFunction(\'' + id + '\')') +
                .product_detail
                    p #{product.snack_name}
                    p #{product.price}
                    p #{product.type}
                    p #{product.add_on}
                    p #{product.size}
                    button.btn(type="add") Add to cart
                br

script.
    function myFunction(id) {
        var x = document.getElementsByClassName("product_detail");
        if (x[id].style.display === "block") {
            x[id].style.display = "none";
        } else {
            x[id].style.display = "block";
        }
    };
script.
    function calculate_total_price(price) {
        var x = price;
        return 1;
    
    };